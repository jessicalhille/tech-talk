<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech-Talk</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
</head>

<body>
  <div class="wrapper">
      <header class="hero">
          <div>
            <nav class="nav">
                    <a class="subtitle" href="/">Home</a>
                    <a class="subtitle" href="/dashboard">Dashboard</a>
                    {{#if loggedIn}}
                    <a class="subtitle" href="/" id="logout">Logout</a>
                    {{else}}
                    <a class="subtitle" href="/login">Login</a>
                    {{/if}}
                </nav>          
          </div>

          <h1 class="title is-1" id="blog-title">Tech-Talk Blog</h1>
      </header>

    <main>
      {{{ body }}}
    </main>
  </div>

    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}

</body>

</html>