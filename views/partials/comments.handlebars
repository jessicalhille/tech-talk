<div class="box" id="comments">
    {{#each this}}
    <section class="comment">
        <div class="field">
            {{user.username}} on {{format_date created_at}}
        </div>
        <div class="field">
            {{comment_text}}
        </div>
    </section>
    {{/each}}
</div>